<div class="container mt-3">
  <h1>New lists</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment%>
    <%= f.association :movie%>
    <%= f.submit "Submit", class: "btn btn-primary"%>
  <% end %>
</div>
